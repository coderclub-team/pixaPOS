import PageContainer from "@/components/layout/page-container";

export default function ReceiptPage() {
  return (
    <PageContainer pageTitle="Receipt" pageDescription="Outlet — Billing receipt">
      <div className="rounded-lg border p-6">
        <p className="text-muted-foreground">Receipt — placeholder for outlet receipt.</p>
      </div>
    </PageContainer>
  );
}
