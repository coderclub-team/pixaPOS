/**
 * Re-export canonical nav config from @pixa/ui to keep single source of truth.
 * Import via `@/config/nav-config` remains supported; prefer `@pixa/ui/config/nav-config` for new code.
 */
export { navGroups } from "@pixa/ui/config/nav-config";
